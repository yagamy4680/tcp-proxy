// Generated by LiveScript 1.2.0
(function(){
  var net, colors, optimist, bunyan, main;
  net = require('net');
  colors = require('colors');
  optimist = require('optimist');
  bunyan = require('bunyan');
  main = function(){
    var opt, log;
    opt = optimist.usage('Usage: $0').alias('r', 'remote').describe('r', 'the remote destination server, e.g. 192.168.0.2:8080').alias('l', 'listen').describe('l', 'listening port for data transmission, e.g. -l 8000').alias('m', 'monitor').describe('m', 'listening port for data monitoring, e.g. -m 8010').boolean(['h']).demand(['r', 'l', 'm']);
    global.argv = opt.argv;
    if (global.argv.h) {
      opt.showHelp();
      process.exit(0);
    }
    log = bunyan.createLogger({
      name: 'tcp-proxy'
    }, {
      serializers: bunyanDebugStream.serializers
    });
    return log.info('hi');
  };
  main();
}).call(this);
